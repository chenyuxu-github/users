replicaCount: 1

image:
  repository: chenyuxu-github/users
  tag: latest
  pullPolicy: IfNotPresent

imagePullSecrets: []

server:
  port: 8080

database:
  host: mysql
  port: 3306
  username: root
  password: "123"
  name: test
  charset: utf8mb4
  parseTime: true
  loc: Local

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: users.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

resources: {}

nodeSelector: {}
tolerations: []
affinity: {}
